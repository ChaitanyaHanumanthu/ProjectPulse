const app = require("./server");

// importing dotenv
require("dotenv").config();
// port = process.env.port; // assigning the port number
// app.listen(port, () => console.log(`server initiated at ${porrt}`));
